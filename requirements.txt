# Road to the Fall Classic — Python pipeline (Phase 1)
# Core data + modeling
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
statsmodels>=0.14
xgboost>=2.0
joblib>=1.3
pyarrow>=14.0          # parquet engine for data/processed
matplotlib>=3.7        # calibration plots

# Data collection
pybaseball>=2.2        # bundles Lahman DB + scrapes Baseball-Reference / FanGraphs
openpyxl>=3.1          # read the legacy .xlsm validation baseline

# Testing
pytest>=7.4

# --- Phase 2 (dashboard API) — install when we get there ---
# fastapi>=0.110
# uvicorn[standard]>=0.27
# apscheduler>=3.10
